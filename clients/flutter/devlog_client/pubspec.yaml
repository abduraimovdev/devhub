name: devlog_client
description: >-
  devhub log ingest klienti — crash / API xato / freeze / login ni tutib,
  POST /v1/log orqali devhub'ga yuboradi. Har loyiha git-dependency bilan ulaydi.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
